project('camera_cv_tests', 'cpp',
  version : '0.1',
  default_options : ['warning_level=3',
                     'cpp_std=c++14'])

exe = executable('camera_cv_tests', 'camera_cv_tests.cpp',
  install : true)

test('basic', exe)
